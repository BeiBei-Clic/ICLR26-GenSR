---
phase: 02-train-data-validate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - dit_train/data/verify_dataset.py
autonomous: true
requirements: [DATA-03]

must_haves:
  truths:
    - "运行 verify_dataset.py 后，输出 prior_mu 和 post_mu 的完整统计（mean, std, min, max）"
    - "运行后输出 KL 散度统计（逐维度均值和标准差）"
    - "运行后输出 diff norm 统计（均值和标准差）"
    - "运行后输出 NaN/Inf 检测结果"
    - "运行后输出自动 PASS/FAIL 判断"
    - "运行后在 dit_train/data/ 目录下生成矢量图 PDF 文件（分布可视化）"
  artifacts:
    - path: "dit_train/data/verify_dataset.py"
      provides: "完整数据验证脚本：收集 1000 样本、统计计算、KL 散度、NaN/Inf 检测、PASS/FAIL 判断、分布可视化（PDF 矢量图）"
      min_lines: 80
  key_links:
    - from: "dit_train/data/verify_dataset.py"
      to: "dit_train/data/latent_dataset.py"
      via: "import create_latent_dataloader"
      pattern: "from dit_train.data.latent_dataset import create_latent_dataloader"
    - from: "dit_train/data/verify_dataset.py"
      to: "dit_train/data/latent_distribution.pdf"
      via: "matplotlib savefig"
      pattern: "savefig.*\\.pdf"
---

<objective>
扩展 verify_dataset.py，在已有基础验证逻辑上增加完整的训练数据质量验证功能。

Purpose: 确认 LatentPairDataset 生成的 (prior_mu, post_mu) 训练对分布合理，无 posterior collapse，数值稳定，Flow Matching 有可学习的信号。

Output: 扩展后的 verify_dataset.py 脚本，运行后输出四项验证结果和自动 PASS/FAIL 判断，并保存分布可视化矢量图 PDF。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-train-data-validate/02-CONTEXT.md
@dit_train/data/verify_dataset.py
@dit_train/data/latent_dataset.py

<interfaces>
<!-- Phase 1 交付物：LatentPairDataset + create_latent_dataloader -->

From dit_train/data/latent_dataset.py:
```python
class LatentPairDataset(IterableDataset):
    def __init__(self, params, batch_size=16, device="cuda", checkpoint_path="weights/checkpoint.pth")

def create_latent_dataloader(params, batch_size=16, num_workers=0, device="cuda", checkpoint_path="weights/checkpoint.pth")
```

<!-- CVAE 训练参数 -->
From parsers.py:
```python
# kl_limits 默认 0.2，影响 posterior collapse 阈值判断
parser.add_argument("--kl_limits", type=float, default=0.2)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 扩展数据收集与统计验证逻辑</name>
  <files>dit_train/data/verify_dataset.py</files>
  <read_first>
    dit_train/data/verify_dataset.py
    dit_train/data/latent_dataset.py
    parsers.py (kl_limits 参数，行 526)
  </read_first>
  <action>
在已有 verify_dataset.py 基础上重写 main() 函数，实现以下功能（per D-01, D-02, D-03, D-04, D-05）：

1. **数据收集**（per D-02）：
   - 保留已有的 params/device 初始化代码
   - 将 n_samples 改为 1000（可配置的局部变量），batch_size 改为 32 以加快收集
   - 用 dataloader 循环收集直到达到 n_samples，而非固定 n_batches
   - 收集 prior_all, post_all 为 numpy 数组，形状 (n_samples, 512)

2. **基础统计检查**（per D-03 第 3 项）：
   - 计算 prior_mu 和 post_mu 的 mean, std, min, max
   - 检测是否有 NaN 或 Inf 值
   - 打印统计结果

3. **KL 散度计算**（per D-03 第 2 项）：
   - 逐维度计算 prior_mu 和 post_mu 之间的 KL 散度（将每维看作高斯分布，用样本 mean/std 估计参数，计算解析 KL）
   - 计算 KL 散度的均值和标准差
   - 打印结果

4. **diff norm 统计**：
   - 计算 ||post_mu - prior_mu||_2 对每个样本
   - 打印均值和标准差

5. **自动 PASS/FAIL 判断**（per D-04, D-05）：
   - 在脚本内部定义阈值常量（不通过命令行）：
     ```python
     KL_THRESHOLD = 0.01   # KL 散度均值阈值
     DIFF_NORM_THRESHOLD = 0.1  # diff norm 均值阈值
     ```
   - PASS 条件：kl_mean > KL_THRESHOLD AND diff_norm_mean > DIFF_NORM_THRESHOLD AND 无 NaN/Inf
   - 打印明确的 PASS/FAIL 结果，FAIL 时说明哪项未通过

6. **保留已有的 `if __name__ == "__main__"` 入口**

注意：
- 不做函数封装（per CLAUDE.md），统计和验证逻辑直接内联在 main() 中
- 不使用 try-except（per CLAUDE.md）
- 使用 scipy.stats 计算高斯 KL 散度，公式为 KL(N1||N2) = log(std2/std1) + (std1^2 + (mean1-mean2)^2)/(2*std2^2) - 0.5，逐维度计算
  - 此处用 KL(posterior || prior) 作为检测 posterior collapse 的指标，即 N1=post_mu 每维分布, N2=prior_mu 每维分布
  - 逐维度计算后取均值和标准差
- 阈值定义在 main() 函数顶部作为局部变量（per D-05）
  </action>
  <acceptance_criteria>
    - verify_dataset.py 中包含 n_samples = 1000 或类似变量（per D-02）
    - 包含 KL_THRESHOLD = 0.01 和 DIFF_NORM_THRESHOLD = 0.1 定义（per D-04, D-05）
    - 包含 NaN/Inf 检测逻辑（调用 np.isnan 和 np.isinf）
    - 包含逐维度 KL 散度计算逻辑
    - 包含 diff norm 计算逻辑（np.linalg.norm）
    - 包含 PASS/FAIL 判断和打印
    - 无函数定义（全部内联在 main() 中）
    - 无 try-except
  </acceptance_criteria>
  <verify>
    <automated>cd /home/xyh/Symbolic_Regression/E2E/ICLR26-GenSR && python -c "
import ast, sys
with open('dit_train/data/verify_dataset.py') as f:
    source = f.read()
tree = ast.parse(source)
checks = {
    'n_samples=1000': 'n_samples = 1000' in source or 'n_samples=1000' in source,
    'KL_THRESHOLD=0.01': 'KL_THRESHOLD = 0.01' in source or 'KL_THRESHOLD=0.01' in source,
    'DIFF_NORM_THRESHOLD=0.1': 'DIFF_NORM_THRESHOLD = 0.1' in source or 'DIFF_NORM_THRESHOLD=0.1' in source,
    'nan_check': 'isnan' in source,
    'inf_check': 'isinf' in source,
    'kl_divergence': 'KL' in source.upper() and ('kl' in source.lower()),
    'pass_fail': 'PASS' in source and 'FAIL' in source,
    'no_try_except': 'try:' not in source and 'except' not in source,
}
for name, ok in checks.items():
    status = 'OK' if ok else 'MISSING'
    print(f'  {status}: {name}')
    if not ok:
        sys.exit(1)
print('All checks passed.')
"</automated>
  </verify>
  <done>verify_dataset.py 包含完整的数据收集（1000 样本）、基础统计、KL 散度计算、diff norm 计算、NaN/Inf 检测和自动 PASS/FAIL 判断逻辑。所有阈值在脚本内部定义。无函数封装、无 try-except。</done>
</task>

<task type="auto">
  <name>Task 2: 添加分布可视化并整合运行验证</name>
  <files>dit_train/data/verify_dataset.py</files>
  <read_first>
    dit_train/data/verify_dataset.py
  </read_first>
  <action>
在 Task 1 扩展后的 verify_dataset.py 中添加分布可视化功能（per D-03 第 1 项, D-06）：

1. **在 main() 的数据收集之后、统计计算之前，添加 matplotlib 可视化代码**：
   - `import matplotlib` 并设后端为 Agg（无头环境）
   - `import matplotlib.pyplot as plt`

2. **创建 3 个子图（1 行 3 列）**：
   - 子图 1：prior_mu 各维度均值的直方图（将 1000 个样本的 prior_mu 取每维均值得到一个 512 维向量，对这个向量画直方图，展示各维度均值的分布）。标题 "prior_mu dimension means"，xlabel "mean value"，ylabel "count"
   - 子图 2：post_mu 各维度均值的直方图（同上，对 post_mu 做）。标题 "post_mu dimension means"，xlabel "mean value"，ylabel "count"
   - 子图 3：diff norm 的直方图（1000 个样本各自的 ||post_mu - prior_mu||_2）。标题 "||post_mu - prior_mu|| distribution"，xlabel "L2 norm"，ylabel "count"

3. **保存为矢量图 PDF**（per D-06）：
   - 保存路径：`dit_train/data/latent_distribution.pdf`
   - 使用 `plt.savefig(path, format='pdf', bbox_inches='tight')`
   - 打印保存路径

4. **运行完整脚本验证**：
   - 执行 `python -m dit_train.data.verify_dataset` 确认端到端运行成功
   - 检查输出包含 "PASS" 或 "FAIL" 以及各项统计数值
   - 检查 PDF 文件生成

注意：
- 可视化代码直接内联在 main() 中（per CLAUDE.md，不做函数封装）
- 直方图 bins=50
- 图的整体标题用 `plt.suptitle("Latent Distribution Validation (n=1000)")`
- tight_layout() 调整布局
  </action>
  <acceptance_criteria>
    - verify_dataset.py 包含 `import matplotlib` 和 `plt.savefig` 调用
    - savefig 使用 `.pdf` 格式（非 .png/.jpg）
    - 包含 3 个子图的创建代码（plt.subplot 或 fig, axes）
    - 包含 latent_distribution.pdf 保存路径
    - 端到端运行 `python -m dit_train.data.verify_dataset` 输出 PASS 或 FAIL
    - dit_train/data/latent_distribution.pdf 文件生成
  </acceptance_criteria>
  <verify>
    <automated>cd /home/xyh/Symbolic_Regression/E2E/ICLR26-GenSR && python -c "
import os, sys
# 检查源码包含可视化相关代码
with open('dit_train/data/verify_dataset.py') as f:
    source = f.read()
checks = {
    'matplotlib_import': 'matplotlib' in source,
    'savefig_pdf': 'savefig' in source and '.pdf' in source,
    'subplot': 'subplot' in source or 'subplots' in source,
    'histogram': 'hist' in source,
    'tight_layout': 'tight_layout' in source,
}
for name, ok in checks.items():
    status = 'OK' if ok else 'MISSING'
    print(f'  {status}: {name}')
    if not ok:
        sys.exit(1)
print('Source code checks passed.')
"</automated>
  </verify>
  <done>verify_dataset.py 包含完整的四项验证功能（分布可视化 PDF 矢量图、KL 散度统计、基础统计检查、自动 PASS/FAIL 判断）。端到端运行成功，输出统计结果和判断，生成 latent_distribution.pdf 矢量图。</done>
</task>

</tasks>

<verification>
1. 源码检查：verify_dataset.py 包含 n_samples=1000、KL_THRESHOLD=0.01、DIFF_NORM_THRESHOLD=0.1、NaN/Inf 检测、KL 散度计算、PASS/FAIL 判断、matplotlib 矢量图保存
2. 运行检查：`python -m dit_train.data.verify_dataset` 端到端运行成功，输出完整统计和 PASS/FAIL 判断
3. 文件检查：`dit_train/data/latent_distribution.pdf` 存在
4. 代码规范检查：无函数封装（全部内联在 main()）、无 try-except、图片为矢量图 PDF 格式
</verification>

<success_criteria>
- `python -m dit_train.data.verify_dataset` 运行成功，退出码 0
- 输出包含 prior_mu/post_mu 基础统计（mean, std, min, max）
- 输出包含 KL 散度统计（逐维度均值和标准差）
- 输出包含 diff norm 统计（均值和标准差）
- 输出包含 NaN/Inf 检测结果
- 输出包含明确的 PASS 或 FAIL 判断
- `dit_train/data/latent_distribution.pdf` 文件存在且为有效 PDF
</success_criteria>

<output>
After completion, create `.planning/phases/02-train-data-validate/02-01-SUMMARY.md`
</output>
