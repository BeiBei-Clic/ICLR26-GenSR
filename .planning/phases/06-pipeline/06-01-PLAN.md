---
phase: 06-pipeline
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - dit_train/pipeline.py
  - tests/test_pipeline.py
autonomous: true
requirements: [INF-02, INF-03]

must_haves:
  truths:
    - "z_opt 能传入 FeatureFusion + Decoder 生成合法 token 序列"
    - "生成的表达式经 BFGS 常数优化后 R^2 有意义（非零非 NaN）"
    - "端到端管线可作为函数调用：输入 (X, Y) → 输出 (expression, R^2)"
  artifacts:
    - path: "dit_train/pipeline.py"
      provides: "dit_inference 端到端推理函数"
      exports: ["dit_inference"]
    - path: "tests/test_pipeline.py"
      provides: "端到端管线单元测试"
      contains: "test_"
  key_links:
    - from: "dit_train/pipeline.py"
      to: "dit_train/inference.py"
      via: "from dit_train.inference import euler_inference"
      pattern: "euler_inference"
    - from: "dit_train/pipeline.py"
      to: "model.py"
      via: "model.encode_only / prepare_latent_for_decoder / generate_from_latent"
      pattern: "encode_only|prepare_latent_for_decoder|generate_from_latent"
    - from: "dit_train/pipeline.py"
      to: "LSO_fit.py"
      via: "from LSO_fit import gen2eq"
      pattern: "gen2eq"
---

<objective>
实现端到端 DiT 推理管线函数 dit_inference。

Purpose: 将 Phase 5 的 euler_inference 与 CVAE 编码、Decoder 解码、BFGS 常数优化串联为完整推理管线，替换 CMA-ES 搜索步骤。

Output: dit_train/pipeline.py（dit_inference 函数）+ tests/test_pipeline.py（单元测试）
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-pipeline/06-CONTEXT.md
@.planning/phases/05-euler/05-01-SUMMARY.md

<interfaces>
<!-- 关键接口：executor 必须使用这些，无需探索代码库 -->

From dit_train/inference.py:
```python
def euler_inference(dit, prior_mu, num_steps=16):
    """Returns z_opt, same shape as prior_mu."""
```

From model.py (VAESymbolicRegressor):
```python
def encode_only(self, samples):
    """Returns (prior_mu, prior_logvar)"""
    # samples 需要包含 'X_scaled_to_fit', 'Y_scaled_to_fit' 键

def prepare_latent_for_decoder(self, z, logvar):
    """Returns src_enc (B, 200, 512)"""
    # 调用 FeatureFusion(mu, logvar)

def generate_from_latent(self, src_enc):
    """Returns (generations, gen_len)"""
    # generations shape: (max_len, B) token id 序列
```

From LSO_fit.py:
```python
def gen2eq(env, params, encoded_y, generations, sample_to_learn, stored_skeletons):
    """token 序列 → 表达式树 → BFGS 优化 → 评估
    Returns: (success, skeleton_candidate, tree, complexity, y, y_pred,
              mse_fit, mse_pred, results_fit, results_predict)
    """
```

From const_opt.py:
```python
def refine(env, X, y, candidates, verbose):
    """BFGS 常数优化。candidates 为表达式树列表。"""
```

From dit_train/train_fm.py:
```python
def load_checkpoint(dit, path, device="cuda"):
    """加载 DiT checkpoint。Returns (step, val_loss, ckpt_dict)"""
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 实现 dit_inference 端到端管线 + 单元测试</name>
  <files>dit_train/pipeline.py, tests/test_pipeline.py</files>
  <read_first>
    dit_train/inference.py
    model.py
    LSO_fit.py (lines 89-156, gen2eq 函数)
    .planning/phases/06-pipeline/06-CONTEXT.md
  </read_first>

  <behavior>
    - Test 1: 给定 mock 模型对象，dit_inference 返回包含 "expression" 和 "r2" 键的 dict
    - Test 2: 返回的 expression 是非空字符串
    - Test 3: 返回的 r2 是有限浮点数（非 NaN、非 Inf）
    - Test 4: 函数调用 model.encode_only 并传入正确的 sample_to_learn 格式
    - Test 5: 函数调用 euler_inference，prior_mu 形状正确
    - Test 6: 函数调用 model.prepare_latent_for_decoder 并使用 z_opt 和 prior_logvar（per D-01）
  </behavior>

  <action>
RED 阶段：创建 tests/test_pipeline.py，包含 6 个测试用例。

测试需要构造 mock 对象来模拟完整管线中的各组件：
- mock model: VAESymbolicRegressor 的 encode_only / prepare_latent_for_decoder / generate_from_latent
- mock dit: GenSRDiT 前向
- mock env: 环境（gen2eq 需要 env, params, sample_to_learn）
- mock gen2eq: 使用 unittest.mock.patch 替换 LSO_fit.gen2eq

Mock model 行为：
- encode_only(sample) → 返回 (prior_mu (1, 512), prior_logvar (1, 512))，用 torch.randn
- prepare_latent_for_decoder(z, logvar) → 返回 src_enc (1, 200, 512)
- generate_from_latent(src_enc) → 返回 (generations (max_len, 1), gen_len)，generations 用随机 token id

Mock gen2eq 行为：
- 返回 (True, mock_skeleton, mock_tree, 5, y, y_pred, 0.01, 0.02, {"r2_zero": [0.95]}, {"r2_zero": [0.93]})

sample_to_learn 格式（per D-04 step 1 和 LSO_fit.py）：
```python
sample = {
    "X_scaled_to_fit": [np.random.randn(50, 2)],
    "Y_scaled_to_fit": [np.random.randn(50, 1)],
    "x_to_fit": [np.random.randn(50, 2)],
    "y_to_fit": [np.random.randn(50, 1)],
    "x_to_predict": [np.random.randn(50, 2)],
    "y_to_predict": [np.random.randn(50, 1)],
}
```

GREEN 阶段：创建 dit_train/pipeline.py，实现 dit_inference 函数。

函数签名（per D-02, D-03）：
```python
def dit_inference(X, y, env, params, model, dit, num_steps=16, verbose=False):
    """端到端 DiT 推理管线。

    Args:
        X: (N, D) numpy array，输入特征
        y: (N, 1) numpy array，目标值
        env: 符号回归环境
        params: 参数对象
        model: VAESymbolicRegressor 实例（已加载权重）
        dit: GenSRDiT 实例（已加载 checkpoint）
        num_steps: Euler 积分步数，默认 16
        verbose: 是否打印详细信息
    Returns:
        dict: {
            "success": bool,
            "expression": str,  # 表达式字符串
            "r2": float,        # R^2 拟合值
            "complexity": int,  # 表达式复杂度
            "tree": object,     # 表达式树
        }
    """
```

管线实现步骤（per D-04）：

Step 1 - 数据预处理：
```python
sample_to_learn = {
    "X_scaled_to_fit": [X],
    "Y_scaled_to_fit": [y],
    "x_to_fit": [X],
    "y_to_fit": [y],
    "x_to_predict": [X],
    "y_to_predict": [y],
}
```

Step 2 - CVAE 编码：
```python
prior_mu, prior_logvar = model.encode_only(sample_to_learn)
```

Step 3 - Euler 积分：
```python
z_opt = euler_inference(dit, prior_mu, num_steps=num_steps)
```

Step 4 - FeatureFusion：
```python
src_enc = model.prepare_latent_for_decoder(z_opt, prior_logvar)
```
注意：per D-01，z_opt 作为 mu，logvar 使用 CVAE 编码时的 prior_logvar。

Step 5 - Decoder 解码：
```python
generations, gen_len = model.generate_from_latent(src_enc)
```

Step 6 - gen2eq 表达式生成 + BFGS 优化：
```python
from LSO_fit import gen2eq
dummy_latent = torch.zeros(1, 1)
eq_outputs = gen2eq(env, params, dummy_latent, generations,
                    sample_to_learn, set())
success, skeleton_candidate, tree, complexity, y_pred_data, y_pred_pred, \
    mse_fit, mse_pred, results_fit, results_predict = eq_outputs
```

返回值组装：
```python
if success:
    return {
        "success": True,
        "expression": tree.infix(),
        "r2": results_fit["r2_zero"][0],
        "complexity": complexity,
        "tree": tree,
    }
else:
    return {
        "success": False,
        "expression": "",
        "r2": 0.0,
        "complexity": -1,
        "tree": None,
    }
```
  </action>

  <verify>
    <automated>cd /home/xyh/Symbolic_Regression/E2E/ICLR26-GenSR && python -m pytest tests/test_pipeline.py -x -v</automated>
  </verify>

  <done>
    - dit_train/pipeline.py 包含 dit_inference 函数，实现完整 6 步管线 (per D-04)
    - z_opt 作为 mu 传入 FeatureFusion，logvar 使用 prior_logvar (per D-01)
    - gen2eq 被正确调用做表达式生成和 BFGS 优化 (INF-03)
    - 6 个测试全部通过
    - 函数接受已加载的模型对象 (per D-03)
  </done>
</task>

</tasks>

<verification>
1. `python -m pytest tests/test_pipeline.py -x -v` — 6 个测试全部通过
2. `python -c "from dit_train.pipeline import dit_inference; print('OK')"` — 导入成功
3. grep -c "euler_inference" dit_train/pipeline.py — 包含 euler_inference 调用 (INF-02)
4. grep -c "gen2eq" dit_train/pipeline.py — 包含 gen2eq 调用 (INF-03)
</verification>

<success_criteria>
1. dit_train/pipeline.py 存在，包含 dit_inference 函数
2. tests/test_pipeline.py 存在，6 个测试全部通过
3. dit_inference 实现 6 步管线流程：数据预处理 → CVAE 编码 → Euler 积分 → FeatureFusion → Decoder → BFGS
4. z_opt 作为 mu、prior_logvar 作为 logvar 传入 FeatureFusion (per D-01)
5. gen2eq 处理 token 序列到表达式 + BFGS 常数优化 (INF-03)
</success_criteria>

<output>
After completion, create `.planning/phases/06-pipeline/06-01-SUMMARY.md`
</output>
