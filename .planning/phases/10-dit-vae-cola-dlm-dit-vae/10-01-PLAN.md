---
phase: 10-dit-vae-cola-dlm-dit-vae
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - dit_train/finetune_lm_head.py
  - tests/test_lm_head_finetune.py
autonomous: true
requirements:
  - DECODE-FT-01
  - DECODE-FT-02
  - DECODE-FT-03

must_haves:
  truths:
    - "冻结 CVAE + DiT + FeatureFusion + Decoder Transformer blocks，只有 lm_head 参数 requires_grad=True"
    - "训练数据走完整 CVAE -> DiT Euler -> FeatureFusion -> Decoder teacher-forcing 链路"
    - "CE loss 在 500 步训练中下降"
    - "微调后的 lm_head checkpoint 保存到磁盘"
  artifacts:
    - path: "dit_train/finetune_lm_head.py"
      provides: "Decoder lm_head 微调训练脚本"
      contains: "def main()"
    - path: "tests/test_lm_head_finetune.py"
      provides: "冻结策略和 CE loss 前向传播测试"
      contains: "test_freeze_strategy"
  key_links:
    - from: "dit_train/finetune_lm_head.py"
      to: "dit_train/inference.py::euler_inference"
      via: "from dit_train.inference import euler_inference"
      pattern: "euler_inference"
    - from: "dit_train/finetune_lm_head.py"
      to: "symbolicregression/model/transformer.py::TransformerModel.predict"
      via: "decoder('predict', tensor=..., pred_mask=..., y=..., get_scores=True)"
      pattern: "lm_head"
---

<objective>
实现 Decoder lm_head 微调训练脚本，冻结所有模块只解冻 lm_head 输出投影层，用 DiT 传输后的 z_opt 作为 Decoder 输入做 teacher-forcing CE loss 训练。

Purpose: 让 Decoder 适应 DiT 传输后的潜向量分布，提升表达式解码质量。照搬 Cola-DLM cola_vae_finetune.py 的冻结/训练模式。
Output: dit_train/finetune_lm_head.py 训练脚本 + tests/test_lm_head_finetune.py 测试
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-dit-vae-cola-dlm-dit-vae/10-CONTEXT.md
@.planning/phases/10-dit-vae-cola-dlm-dit-vae/10-RESEARCH.md

<interfaces>
<!-- 关键接口定义，executor 无需额外探索 -->

From dit_train/inference.py:
```python
def euler_inference(dit, prior_mu, num_steps=16):
    # prior_mu: (B, 512) -> z_opt: (B, 512)
    # 内部已包含 torch.no_grad()
```

From symbolicregression/model/transformer.py (TransformerModel):
```python
# Teacher-forcing forward:
tensor = decoder("fwd", x=eq_tokens, lengths=eq_lengths, causal=True, src_enc=src_enc, src_len=None, use_cache=False)
# tensor: (slen, B, 512)

# Predict with CE loss:
scores, loss = decoder("predict", tensor=tensor, pred_mask=pred_mask, y=y, get_scores=True)
# loss: scalar CE loss
```

From symbolicregression/model/transformer.py line 1336-1340:
```python
self.lm_head = nn.Linear(self.dim, self.n_words, bias=True)  # 512 -> 10292
if params.share_inout_emb:
    self.lm_head.weight = self.tok_embed.weight  # 共享权重
```

From symbolicregression/trainer_vae.py line 893-896 (pred_mask 构造):
```python
alen = torch.arange(params.max_src_len, dtype=torch.long, device=len2.device)
pred_mask = (alen[:, None] < len2[None] - 1)
y = x2[1:].masked_select(pred_mask[:-1])
```

From symbolicregression/model/feature_fusion.py:
```python
class FeatureFusion(nn.Module):
    def forward(self, mu: torch.Tensor, logvar: torch.Tensor) -> torch.Tensor:
        # mu: (B, 512), logvar: (B, 512) -> (B, 200, 512)
```

From dit_train/data/latent_dataset.py (数据生成模式):
```python
# LatentPairDataset._lazy_init 中的 env + modules 初始化模式
# gen_expr -> embedder_f -> batch_equations -> embedder_e -> vae_model forward
```

From symbolicregression/envs/environment.py:
```python
def batch_equations(self, equations, max_len=200):
    # equations: list of LongTensor -> sent: (max_len, B), lengths: (B,)
    # sent[0] = EOS, sent[1:-1] = eq tokens, sent[-1] = EOS

def word_to_idx(self, words, float_input=True):
    # float_input=False 时: list of token lists -> list of LongTensor
```

From dit_train/model.py:
```python
class GenSRDiT(nn.Module):
    def forward(self, z_t, t, prior_mu):
        # z_t: (B, 512), t: (B,), prior_mu: (B, 512) -> (B, 512)
```

From symbolicregression/model/__init__.py:
```python
def build_modules(env, params, mode='train') -> dict:
    # returns {"data_encoder": ..., "cvae": ..., "token_embed": ..., "seq_decoder": ..., "feature_fusion": ...}

def reload_model(modules, modules_to_load, path=None, requires_grad=True):
    # 从 checkpoint 加载指定模块权重
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 实现 lm_head 微调训练脚本 + 冻结策略测试</name>
  <files>dit_train/finetune_lm_head.py, tests/test_lm_head_finetune.py</files>
  <read_first>
    - dit_train/finetune_lm_head.py (新文件，不存在则跳过)
    - Cola-DLM-main/scripts/cola_vae_finetune.py
    - symbolicregression/trainer_vae.py (line 840-930, teacher-forcing CE loss 计算模式)
    - dit_train/data/latent_dataset.py (env + modules 初始化模式)
    - dit_train/inference.py (euler_inference)
    - dit_train/train_fm.py (训练循环风格参考)
    - symbolicregression/model/__init__.py (build_modules / reload_model)
  </read_first>
  <behavior>
    - Test 1 (test_freeze_strategy): 构建所有模块后，执行冻结 + 解冻 lm_head 逻辑，断言只有 lm_head 的 weight 和 bias 有 requires_grad=True，其他所有模块的参数 requires_grad=False
    - Test 2 (test_ce_loss_forward): 用 mock 数据（随机 x1/x2_e/len1/len2），走完 CVAE -> DiT Euler -> FeatureFusion -> Decoder fwd+predict 链路，断言 loss 是有限标量、loss > 0、loss 可 backward
  </behavior>
  <action>
创建两个文件：

### 文件 1: tests/test_lm_head_finetune.py

测试冻结策略和 CE loss 前向传播：

```python
import torch
import pytest
import os
import sys

_ROOT = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))
if _ROOT not in sys.path:
    sys.path.insert(0, _ROOT)


@pytest.fixture(scope="module")
def modules_and_env():
    """初始化 env + 所有模块（冻结 + 加载 checkpoint），只在模块级别初始化一次。"""
    from parsers import get_parser
    params = get_parser().parse_args([])
    params.max_input_dimension = 10
    params.device = "cuda:0" if torch.cuda.is_available() else "cpu"

    import symbolicregression.utils
    symbolicregression.utils.CUDA = not params.cpu

    from symbolicregression.envs import build_env
    from symbolicregression.model import build_modules, reload_model

    env = build_env(params)
    modules = build_modules(env, params)

    # 加载 CVAE + data_encoder + token_embed + seq_decoder + feature_fusion
    reload_model(modules,
        modules_to_load=["cvae", "data_encoder", "token_embed", "seq_decoder", "feature_fusion"],
        path="weights/checkpoint.pth",
        requires_grad=False)

    return modules, env, params


def test_freeze_strategy(modules_and_env):
    """D-01: 只有 lm_head 参数 requires_grad=True，其他全部冻结。"""
    modules, env, params = modules_and_env
    decoder = modules["seq_decoder"]

    # 冻结所有模块
    for mod in modules.values():
        for p in mod.parameters():
            p.requires_grad = False

    # 解冻 lm_head (per D-01)
    for p in decoder.lm_head.parameters():
        p.requires_grad = True

    # 验证：只有 lm_head 参数可训练
    trainable_names = []
    frozen_count = 0
    for name, p in decoder.named_parameters():
        if p.requires_grad:
            trainable_names.append(name)
        else:
            frozen_count += 1

    assert len(trainable_names) > 0, "应该有可训练参数"
    for name in trainable_names:
        assert "lm_head" in name, f"只有 lm_head 应该可训练，但 {name} 是可训练的"
    assert frozen_count > 0, "应该有冻结的参数"


def test_ce_loss_forward(modules_and_env):
    """完整链路 CVAE -> DiT Euler -> FeatureFusion -> Decoder fwd+predict 产生有效 CE loss。"""
    modules, env, params = modules_and_env
    device = params.device

    # 冻结策略 (D-01)
    for mod in modules.values():
        for p in mod.parameters():
            p.requires_grad = False

    # 加载 DiT
    from dit_train.model import GenSRDiT
    dit = GenSRDiT().to(device)
    dit.load_state_dict(torch.load("dit_train/checkpoints/fm_best.pt", map_location=device))
    dit.eval()
    for p in dit.parameters():
        p.requires_grad = False

    # 解冻 lm_head (D-01)
    decoder = modules["seq_decoder"]
    for p in decoder.lm_head.parameters():
        p.requires_grad = True

    embedder_f = modules["data_encoder"]
    embedder_e = modules["token_embed"]
    vae_model = modules["cvae"]
    feature_fusion = modules["feature_fusion"]

    # 生成一个 batch 的训练数据
    import numpy as np
    env.rng = np.random.RandomState(42)

    samples, _ = env.gen_expr(train=True)
    x_to_fit = samples["X_to_fit"]
    y_to_fit = samples["Y_to_fit"]
    x1 = [[[x, y] for x, y in zip(xs, ys)] for xs, ys in zip(x_to_fit, y_to_fit)]
    x1, len1 = embedder_f(x1)

    x2, len2 = env.batch_equations(
        env.word_to_idx([samples["tree_encoded"]], float_input=False)
    )
    from symbolicregression.utils import to_cuda
    x2, len2 = to_cuda(x2, len2)
    x2_e = embedder_e(x2.transpose(0, 1)).transpose(0, 1)

    # Step 1: CVAE 编码 -> prior_mu, prior_logvar
    with torch.no_grad():
        prior_mu, _, post_mu, _, _, _, _ = vae_model(x1, x2_e, len1, len2, mode="train")

    # Step 2: DiT Euler 积分 -> z_opt
    from dit_train.inference import euler_inference
    with torch.no_grad():
        z_opt = euler_inference(dit, prior_mu, num_steps=16)

    # Step 3: FeatureFusion -> src_enc
    with torch.no_grad():
        # 注意：prior_logvar 从 CVAE 编码获取，这里用 post_logvar 的位置取 prior_logvar
        # 从 vae_model forward 的输出顺序: prior_mu, prior_logvar, post_mu, post_logvar, kl_loss, kl_weights, kld
        prior_mu2, prior_logvar, _, _, _, _, _ = vae_model(x1, x2_e, len1, len2, mode="train")
        src_enc = feature_fusion(z_opt, prior_logvar)

    # Step 4: Decoder teacher-forcing -> CE loss
    alen = torch.arange(params.max_src_len, dtype=torch.long, device=len2.device)
    pred_mask = (alen[:, None] < len2[None] - 1)
    y = x2[1:].masked_select(pred_mask[:-1])

    tensor = decoder("fwd", x=x2, lengths=len2, causal=True, src_enc=src_enc, src_len=None, use_cache=False)
    scores, loss = decoder("predict", tensor=tensor, pred_mask=pred_mask, y=y, get_scores=True)

    assert loss.dim() == 0, f"loss 应该是标量，实际 dim={loss.dim()}"
    assert torch.isfinite(loss), f"loss 应该是有限值，实际={loss.item()}"
    assert loss.item() > 0, f"CE loss 应该 > 0，实际={loss.item()}"

    # 验证 loss 可 backward（梯度流到 lm_head）
    loss.backward()
    assert decoder.lm_head.weight.grad is not None, "lm_head.weight 应该有梯度"
    assert decoder.lm_head.bias.grad is not None, "lm_head.bias 应该有梯度"
  ```

### 文件 2: dit_train/finetune_lm_head.py

独立训练脚本，照搬 cola_vae_finetune.py 的模式：

```python
"""Decoder lm_head 微调训练脚本

冻结 CVAE + DiT + FeatureFusion + Decoder Transformer blocks，
只解冻 Decoder 的 lm_head 输出投影层，用 DiT 传输后的 z_opt 做 teacher-forcing CE loss 训练。

照搬 Cola-DLM cola_vae_finetune.py 冻结/训练策略 (D-01, D-03)。
数据流：FunctionEnvironment -> CVAE -> DiT Euler -> FeatureFusion -> Decoder fwd+predict -> CE loss (D-02)。

Usage:
    python dit_train/finetune_lm_head.py
    python dit_train/finetune_lm_head.py --num-iterations 500 --batch-size 8 --learning-rate 1e-4
"""

import os
import sys
import time

_ROOT = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))
if _ROOT not in sys.path:
    sys.path.insert(0, _ROOT)

import numpy as np
import torch
import torch.nn.functional as F
from tqdm import tqdm


def main():
    import argparse
    parser = argparse.ArgumentParser(description="GenSR Decoder lm_head 微调")
    parser.add_argument("--num-iterations", type=int, default=500)
    parser.add_argument("--batch-size", type=int, default=8)
    parser.add_argument("--learning-rate", type=float, default=1e-4)
    parser.add_argument("--dit-checkpoint", type=str, default="dit_train/checkpoints/fm_best.pt")
    parser.add_argument("--vae-checkpoint", type=str, default="weights/checkpoint.pth")
    parser.add_argument("--output-dir", type=str, default="dit_train/checkpoints")
    parser.add_argument("--dit-num-steps", type=int, default=16, help="DiT Euler 积分步数")
    parser.add_argument("--log-every", type=int, default=50)
    args = parser.parse_args()

    device = "cuda" if torch.cuda.is_available() else "cpu"

    # ========== 1. 初始化 env + modules ==========
    import symbolicregression.utils
    symbolicregression.utils.CUDA = device == "cuda"

    from parsers import get_parser
    params = get_parser().parse_args([])
    params.max_input_dimension = 10
    params.device = device

    from symbolicregression.envs import build_env
    from symbolicregression.model import build_modules, reload_model
    from symbolicregression.utils import to_cuda

    env = build_env(params)
    env.rng = np.random.RandomState()

    modules = build_modules(env, params)
    reload_model(
        modules,
        modules_to_load=["cvae", "data_encoder", "token_embed", "seq_decoder", "feature_fusion"],
        path=args.vae_checkpoint,
        requires_grad=False,
    )

    embedder_f = modules["data_encoder"]
    embedder_e = modules["token_embed"]
    vae_model = modules["cvae"]
    decoder = modules["seq_decoder"]
    feature_fusion = modules["feature_fusion"]

    # ========== 2. 加载 DiT ==========
    from dit_train.model import GenSRDiT
    from dit_train.inference import euler_inference

    dit = GenSRDiT().to(device)
    dit.load_state_dict(torch.load(args.dit_checkpoint, map_location=device))
    dit.eval()

    # ========== 3. 冻结策略 (D-01) ==========
    # 冻结所有模块
    for mod in modules.values():
        for p in mod.parameters():
            p.requires_grad = False
    for p in dit.parameters():
        p.requires_grad = False

    # 解冻 lm_head (per D-01, 与 Cola cola_vae_finetune.py 一致)
    for p in decoder.lm_head.parameters():
        p.requires_grad = True

    # 设置 eval 模式
    vae_model.eval()
    dit.eval()
    feature_fusion.eval()
    # Decoder blocks 冻结但 lm_head 需要训练，整体保持 train 让 dropout 等不生效
    # 但 blocks 内部参数冻结所以实际只影响 lm_head

    total_params = sum(p.numel() for p in decoder.parameters())
    trainable_params = sum(p.numel() for p in decoder.parameters() if p.requires_grad)
    print(f"Decoder: {total_params:,} total, {trainable_params:,} trainable ({100*trainable_params/total_params:.2f}%)")

    # ========== 4. Optimizer (D-03, 照搬 cola_vae_finetune.py) ==========
    optimizer = torch.optim.AdamW(
        [p for p in decoder.parameters() if p.requires_grad],
        lr=args.learning_rate,
    )

    # ========== 5. 训练循环 ==========
    print(f"\n=== Decoder lm_head Fine-tuning ===")
    print(f"iterations: {args.num_iterations}, batch_size: {args.batch_size}, lr: {args.learning_rate}")
    print(f"dit_num_steps: {args.dit_num_steps}")
    print()

    best_loss = float("inf")

    for step in range(args.num_iterations):
        t0 = time.time()

        # --- 在线生成训练数据 (D-02) ---
        x1_list, len1_list = [], []
        x2_list, len2_list = [], []
        prior_mu_list, prior_logvar_list = [], []

        with torch.no_grad():
            for _ in range(args.batch_size):
                samples, _ = env.gen_expr(train=True)

                x_to_fit = samples["X_to_fit"]
                y_to_fit = samples["Y_to_fit"]
                x1 = [[[x, y] for x, y in zip(xs, ys)]
                       for xs, ys in zip(x_to_fit, y_to_fit)]
                x1_single, len1_single = embedder_f(x1)

                x2_single, len2_single = env.batch_equations(
                    env.word_to_idx([samples["tree_encoded"]], float_input=False)
                )
                x2_single, len2_single = to_cuda(x2_single, len2_single)
                x2_e_single = embedder_e(x2_single.transpose(0, 1)).transpose(0, 1)

                # CVAE 编码
                prior_mu, prior_logvar, _, _, _, _, _ = vae_model(
                    x1_single, x2_e_single, len1_single, len2_single, mode="train"
                )

                # DiT Euler 积分 -> z_opt
                z_opt = euler_inference(dit, prior_mu, num_steps=args.dit_num_steps)

                # FeatureFusion -> src_enc
                src_enc = feature_fusion(z_opt, prior_logvar)

                x1_list.append(x1_single)
                len1_list.append(len1_single)
                x2_list.append(x2_single)
                len2_list.append(len2_single)
                prior_mu_list.append(src_enc)

        # 合并 batch（这里每个样本的 eq_tokens 和 src_enc 需要一起使用）
        # 由于不同样本的方程长度不同，逐样本做 teacher-forcing 然后平均 loss
        # 或者用 batch_equations 统一 padding
        # 实际上更简单：逐样本计算 loss 然后平均
        # 但为了效率，可以构造 padded batch
        # 这里用逐样本方式，简单直接

        total_loss = 0.0
        for i in range(args.batch_size):
            src_enc = prior_mu_list[i]  # (1, 200, 512) from FeatureFusion
            eq_tokens = x2_list[i]       # (slen, 1)
            eq_len = len2_list[i]        # (1,)

            alen = torch.arange(params.max_src_len, dtype=torch.long, device=device)
            pred_mask = (alen[:, None] < eq_len[None] - 1)
            y = eq_tokens[1:].masked_select(pred_mask[:-1])

            if y.numel() == 0:
                continue

            # Decoder forward (teacher-forcing) — 这里需要梯度流到 lm_head
            tensor = decoder(
                "fwd",
                x=eq_tokens,
                lengths=eq_len,
                causal=True,
                src_enc=src_enc,
                src_len=None,
                use_cache=False,
            )
            scores, loss = decoder(
                "predict",
                tensor=tensor,
                pred_mask=pred_mask,
                y=y,
                get_scores=True,
            )
            total_loss += loss

        avg_loss = total_loss / args.batch_size
        avg_loss.backward()
        optimizer.step()
        optimizer.zero_grad(set_to_none=True)

        dt = time.time() - t0

        # 保存 best checkpoint
        if avg_loss.item() < best_loss:
            best_loss = avg_loss.item()
            save_path = os.path.join(args.output_dir, "lm_head_best.pt")
            os.makedirs(args.output_dir, exist_ok=True)
            torch.save(decoder.lm_head.state_dict(), save_path)

        if step % args.log_every == 0 or step == args.num_iterations - 1:
            print(f"step {step:04d} | loss: {avg_loss.item():.4f} | best: {best_loss:.4f} | dt: {dt*1000:.0f}ms")

    print(f"\nTraining complete. Best loss: {best_loss:.4f}")
    print(f"Saved best lm_head to: {os.path.join(args.output_dir, 'lm_head_best.pt')}")


if __name__ == "__main__":
    main()
```

**关键实现细节：**

1. **冻结策略 (D-01):** 所有模块 `requires_grad=False`，只解冻 `decoder.lm_head.parameters()`。CVAE/DiT/FeatureFusion 保持 `eval()` 模式。Decoder 整体不需要 `.train()` 因为冻结层不影响。

2. **数据流 (D-02):** 在线随机生成样本，CVAE 编码 -> DiT Euler -> FeatureFusion -> Decoder teacher-forcing。冻结部分全部在 `torch.no_grad()` 中执行，只有 Decoder forward 打开 autograd 让梯度流到 lm_head。

3. **训练参数 (D-03):** AdamW lr=1e-4, batch_size=8, 500 iterations。照搬 cola_vae_finetune.py。

4. **CE loss 计算:** 参照 trainer_vae.py line 893-921 的 pred_mask 构造和 predict 调用方式。

5. **显存优化:** CVAE/DiT/FeatureFusion 在 `torch.no_grad()` 中执行，不保存中间激活。

6. **DiT checkpoint 路径:** `dit_train/checkpoints/fm_best.pt`（不是 `weights/fm_best.pth`，per RESEARCH Pitfall 3）。

7. **lm_head 保存:** 只保存 `decoder.lm_head.state_dict()`，包含 weight (10292, 512) 和 bias (10292,)。share_inout_emb 的 tok_embed 也会被间接更新。
  </action>
  <verify>
    <automated>cd /home/xyh/Symbolic_Regression/E2E/ICLR26-GenSR && python -m pytest tests/test_lm_head_finetune.py -x -v 2>&1 | tail -30</automated>
  </verify>
  <done>
    - test_freeze_strategy 通过：只有 lm_head 参数 requires_grad=True
    - test_ce_loss_forward 通过：完整链路产生有效有限 CE loss，loss 可 backward 到 lm_head
    - dit_train/finetune_lm_head.py 可执行（语法正确，import 无误）
  </done>
</task>

<task type="auto">
  <name>Task 2: 试运行训练脚本验证 loss 下降</name>
  <files>dit_train/finetune_lm_head.py</files>
  <read_first>
    - dit_train/finetune_lm_head.py (Task 1 创建)
  </read_first>
  <action>
运行训练脚本 100 步（不是全部 500 步），验证 loss 确实在下降。这是 smoke test：

```bash
cd /home/xyh/Symbolic_Regression/E2E/ICLR26-GenSR && python dit_train/finetune_lm_head.py --num-iterations 100 --log-every 10
```

如果运行中出现任何错误（如 OOM、维度不匹配、NaN loss），回到 Task 1 修复代码。

预期输出：
- step 0000 的 loss 大约在 ln(10292) ≈ 9.2 附近（随机初始化 lm_head 在 10292 类上的交叉熵）
- loss 应该在 100 步内从初始值下降至少 0.5 以上
- 如果 loss 是 NaN 或者不下降，说明有 bug

运行完成后，恢复 lm_head_best.pt 到 Decoder 中验证加载正常：
```python
import torch
state_dict = torch.load("dit_train/checkpoints/lm_head_best.pt", map_location="cpu")
assert "weight" in state_dict
assert "bias" in state_dict
assert state_dict["weight"].shape == (10292, 512)
assert state_dict["bias"].shape == (10292,)
print("lm_head checkpoint 加载验证通过")
```
  </action>
  <verify>
    <automated>cd /home/xyh/Symbolic_Regression/E2E/ICLR26-GenSR && python -c "import torch; sd = torch.load('dit_train/checkpoints/lm_head_best.pt', map_location='cpu'); assert 'weight' in sd and 'bias' in sd; assert sd['weight'].shape == (10292, 512); assert sd['bias'].shape == (10292,); print('OK')" 2>&1</automated>
  </verify>
  <done>
    - 训练脚本 100 步运行完成，loss 从初始值下降
    - lm_head_best.pt 保存到 dit_train/checkpoints/，包含正确形状的 weight 和 bias
  </done>
</task>

</tasks>

<verification>
1. `pytest tests/test_lm_head_finetune.py -x -v` 全部通过
2. `python dit_train/finetune_lm_head.py --num-iterations 100 --log-every 10` 运行完成，loss 下降
3. `dit_train/checkpoints/lm_head_best.pt` 存在且格式正确
</verification>

<success_criteria>
- 冻结策略正确：只有 lm_head 的 2 个参数（weight + bias）requires_grad=True
- CE loss 前向传播完整链路跑通：CVAE -> DiT Euler -> FeatureFusion -> Decoder fwd+predict
- 100 步试运行 loss 下降
- checkpoint 保存并可正确加载
</success_criteria>

<output>
After completion, create `.planning/phases/10-dit-vae-cola-dlm-dit-vae/10-01-SUMMARY.md`
</output>
