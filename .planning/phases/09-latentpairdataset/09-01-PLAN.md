---
phase: 09-latentpairdataset
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - dit_train/data/latent_dataset.py
  - dit_train/train_fm.py
  - tests/test_latent_dataset.py
autonomous: true
requirements:
  - PERF-01
  - PERF-02
  - PERF-03

must_haves:
  truths:
    - "DataLoader spawn 模式能启动多个 worker 并行生成数据，不报 CUDA 错误"
    - "每个 worker 内部延迟初始化 CVAE 模型到 GPU，独立 CUDA context"
    - "Dataset __iter__ yield 完整 batch (B,512) 张量对，跳过 DataLoader collate"
    - "训练循环 next(data_iter) 取到的数据已在 GPU 上，无需 .to(device)"
    - "4 卡 DDP 场景下每张卡独立创建自己的 DataLoader 和 worker"
  artifacts:
    - path: "dit_train/data/latent_dataset.py"
      provides: "spawn 多进程 LatentPairDataset + create_latent_dataloader"
      contains: "_lazy_init"
    - path: "dit_train/train_fm.py"
      provides: "训练循环适配 spawn DataLoader"
      contains: "num_workers"
    - path: "tests/test_latent_dataset.py"
      provides: "spawn DataLoader 测试 + 吞吐量对比测试"
      contains: "test_spawn_dataloader"
  key_links:
    - from: "dit_train/train_fm.py"
      to: "dit_train/data/latent_dataset.py"
      via: "create_latent_dataloader 调用"
      pattern: "create_latent_dataloader.*num_workers"
    - from: "dit_train/data/latent_dataset.py"
      to: "DataLoader"
      via: "multiprocessing_context='spawn' + batch_size=None"
      pattern: "multiprocessing_context.*spawn"
---

<objective>
重写 LatentPairDataset 支持 spawn 多进程并行数据生成，消除 GPU 训练时的数据供给瓶颈。

Purpose: 增大 batch_size 后 GPU 利用率反而下降，因为串行数据生成跟不上。spawn 模式让多个 worker 并行生成数据，yield 完整 batch 跳过 collate 开销。

Output: 改造后的 LatentPairDataset (spawn + lazy init + yield batch) + 适配的训练循环 + 新增测试
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-latentpairdataset/09-CONTEXT.md
@.planning/phases/09-latentpairdataset/09-RESEARCH.md

<interfaces>
<!-- Executor 需要的现有接口 -->

From dit_train/data/latent_dataset.py (当前实现):
```python
class LatentPairDataset(IterableDataset):
    def __init__(self, params, batch_size=16, device="cuda", checkpoint_path="weights/checkpoint.pth")
    def __iter__(self)  # yield (prior_mu_i, post_mu_i) 逐样本

def create_latent_dataloader(params, batch_size=16, num_workers=0, device="cuda", checkpoint_path="weights/checkpoint.pth")
```

From dit_train/train_fm.py (训练循环数据使用方式):
```python
# 行 270-274: DataLoader 创建
loader = create_latent_dataloader(
    params, batch_size=args.device_batch_size,
    checkpoint_path=args.checkpoint_path,
)
data_iter = iter(loader)

# 行 287-290: 训练循环中取数据
for micro_step in range(args.grad_accum_steps):
    prior_mu, post_mu = next(data_iter)
    prior_mu = prior_mu.to(device)
    post_mu = post_mu.to(device)

# 行 160-177: evaluate 函数也使用 data_iter
def evaluate(dit, data_iter, eval_steps):
    for _ in range(eval_steps):
        prior_mu, post_mu = next(data_iter)
        loss, _ = flow_matching_step(dit, prior_mu, post_mu)
```

From symbolicregression/envs/generators.py:
```python
env.gen_expr(train=True) -> (samples, _) 其中 samples 包含 X_to_fit, Y_to_fit, tree_encoded
```

From symbolicregression/model/cvae.py:
```python
vae_model(x1, x2_e, len1, len2, mode="train") -> (prior_mu, _, post_mu, _, _, _, _)
```

From symbolicregression/utils.py:
```python
to_cuda(x2, len2)  # 将张量移到 CUDA
symbolicregression.utils.CUDA  # 全局 CUDA 标志
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 重写 LatentPairDataset 支持 spawn 多进程 + yield batch</name>
  <files>dit_train/data/latent_dataset.py</files>
  <read_first>
    dit_train/data/latent_dataset.py (当前实现，需要完整重写)
    .planning/phases/09-latentpairdataset/09-RESEARCH.md (spawn 方案和代码示例)
    .planning/phases/09-latentpairdataset/09-CONTEXT.md (D-01 到 D-06 决策)
  </read_first>
  <action>
重写 `dit_train/data/latent_dataset.py`，将 LatentPairDataset 从"__init__ 加载模型 + __iter__ 逐样本 yield"改为"__init__ 只保存配置 + __iter__ 首次调用时延迟初始化模型 + yield 完整 batch"。

具体改动：

**1. LatentPairDataset.__init__ 改为只保存配置参数：**
- 保存 self.params, self.batch_size, self.device, self.checkpoint_path
- 添加 self._initialized = False
- 不再调用 build_env, build_modules, reload_model（这些移到 _lazy_init）
- 不再设置 symbolicregression.utils.CUDA（移到 _lazy_init）
- 不再处理 params.tasks 的 list->str 转换（移到 _lazy_init）

**2. 新增 _lazy_init 方法：**
```python
def _lazy_init(self):
    import symbolicregression.utils
    symbolicregression.utils.CUDA = not self.params.cpu

    params = self.params
    if isinstance(params.tasks, list):
        params.tasks = ",".join(params.tasks)

    env = build_env(params)
    env.rng = np.random.RandomState()
    self.env = env

    modules = build_modules(env, params)
    reload_model(
        modules,
        modules_to_load=["cvae", "data_encoder", "token_embed"],
        path=self.checkpoint_path,
        requires_grad=False,
    )

    self.embedder_f = modules["data_encoder"]
    self.embedder_e = modules["token_embed"]
    self.vae_model = modules["cvae"]
    self.vae_model.eval()
    for module in [self.vae_model, self.embedder_f, self.embedder_e]:
        for param in module.parameters():
            param.requires_grad = False
    self._initialized = True
```

**3. __iter__ 改为 yield 完整 batch（per D-05, D-06）：**
```python
def __iter__(self):
    if not self._initialized:
        self._lazy_init()
    while True:
        with torch.no_grad():
            prior_list, post_list = [], []
            for _ in range(self.batch_size):
                samples, _ = self.env.gen_expr(train=True)
                x_to_fit = samples["X_to_fit"]
                y_to_fit = samples["Y_to_fit"]
                x1 = [[[x, y] for x, y in zip(xs, ys)]
                       for xs, ys in zip(x_to_fit, y_to_fit)]
                x1, len1 = self.embedder_f(x1)
                x2, len2 = self.env.batch_equations(
                    self.env.word_to_idx([samples["tree_encoded"]], float_input=False)
                )
                x2, len2 = to_cuda(x2, len2)
                x2_e = self.embedder_e(x2.transpose(0, 1)).transpose(0, 1)
                prior_mu, _, post_mu, _, _, _, _ = self.vae_model(
                    x1, x2_e, len1, len2, mode="train"
                )
                prior_list.append(prior_mu.clone())
                post_list.append(post_mu.clone())
            yield (torch.cat(prior_list, dim=0), torch.cat(post_list, dim=0))
```
注意：yield 整个 batch 的 (B, 512) 张量对，不再逐样本 yield。

**4. create_latent_dataloader 改为 spawn 模式（per D-01, D-05）：**
```python
def create_latent_dataloader(params, batch_size=16, num_workers=2,
                              device="cuda", checkpoint_path="weights/checkpoint.pth"):
    dataset = LatentPairDataset(
        params, batch_size=batch_size,
        device=device, checkpoint_path=checkpoint_path,
    )
    if num_workers > 0:
        return DataLoader(
            dataset,
            batch_size=None,                  # D-05: 跳过 collate，dataset 已 yield 完整 batch
            num_workers=num_workers,
            multiprocessing_context='spawn',  # CUDA 安全（fork 在主进程已初始化 CUDA 时不可用）
            persistent_workers=True,          # 避免重复加载模型（641MB/worker）
            pin_memory=False,                 # 已在 GPU 上，无需 pin
        )
    else:
        return DataLoader(
            dataset,
            batch_size=None,
            num_workers=0,
            pin_memory=False,
        )
```
默认 num_workers=2（per RESEARCH.md 显存预算推荐值：RTX 3090 上 2 worker 额外 1.3GB，安全）。

**5. 保持 backward compatibility：**
- LatentPairDataset 构造函数签名不变（params, batch_size, device, checkpoint_path）
- create_latent_dataloader 签名只新增 num_workers 参数（默认从 0 改为 2）
- num_workers=0 时走纯主进程模式，行为与旧版一致（但 batch_size=None + yield 完整 batch）
  </action>
  <verify>
    <automated>cd /home/xyh/Symbolic_Regression/E2E/ICLR26-GenSR && python3 -c "from dit_train.data.latent_dataset import LatentPairDataset, create_latent_dataloader; print('import OK')"</automated>
  </verify>
  <done>
    - LatentPairDataset.__init__ 不再初始化 GPU 模型，只保存配置参数
    - _lazy_init 方法存在，延迟初始化 env + modules + 冻结权重
    - __iter__ yield 完整 batch (B, 512) 而非逐样本 (512,)
    - create_latent_dataloader 使用 multiprocessing_context='spawn', batch_size=None, persistent_workers=True
    - 默认 num_workers=2
    - import 无报错
  </done>
</task>

<task type="auto">
  <name>Task 2: 适配训练循环 + 更新测试</name>
  <files>dit_train/train_fm.py, tests/test_latent_dataset.py</files>
  <read_first>
    dit_train/train_fm.py (训练循环，行 270-290 需修改)
    tests/test_latent_dataset.py (现有测试，需要新增和修改)
    dit_train/data/latent_dataset.py (Task 1 完成后的新接口)
  </read_first>
  <action>
**Part A: 适配 train_fm.py 训练循环**

修改 `dit_train/train_fm.py` 的两处：

**1. DataLoader 创建处（行 270-273）：**
将：
```python
loader = create_latent_dataloader(
    params, batch_size=args.device_batch_size,
    checkpoint_path=args.checkpoint_path,
)
```
改为：
```python
loader = create_latent_dataloader(
    params, batch_size=args.device_batch_size,
    num_workers=2,
    checkpoint_path=args.checkpoint_path,
)
```

**2. 训练循环取数据处（行 288-290）：**
将：
```python
prior_mu, post_mu = next(data_iter)
prior_mu = prior_mu.to(device)
post_mu = post_mu.to(device)
```
改为：
```python
prior_mu, post_mu = next(data_iter)
```
移除 `.to(device)` 调用，因为 spawn worker 生成的数据已经在 worker 的 GPU 上。DataLoader 的 IPC 会将 GPU 张量传回主进程时保持设备一致。

注意：evaluate 函数（行 173）中的 `next(data_iter)` 不需要改，因为数据已经在 GPU 上。

**Part B: 更新 tests/test_latent_dataset.py**

现有测试需要适配新的 yield batch 行为，并新增 spawn 模式测试：

**1. 修改现有测试适配 yield batch：**
- `test_latent_pair_shapes`：需要创建 DataLoader 后取数据，不再直接 iter(dataset) 取单样本（因为 dataset 现在 yield 完整 batch）
  - 修改为用 `create_latent_dataloader(params, batch_size=4, num_workers=0)` 创建 loader
  - 断言 `prior_mu.shape == (4, 512)` 和 `post_mu.shape == (4, 512)`
- `test_latent_pair_nontrivial`：同样改为从 loader 取 batch，断言 batch 中所有样本非零且 prior != post
- `test_dataloader_iteration`：保持不变（已经用 create_latent_dataloader）
- `test_batch_size`：保持不变（已经用 create_latent_dataloader）
- `test_cva_frozen`：需要调整 -- 因为 __init__ 不再加载模型，需要先触发一次迭代来初始化模型后再检查参数

**2. 新增 spawn 模式测试（标注 @pytest.mark.slow）：**

```python
@pytest.mark.slow
def test_spawn_dataloader(params):
    """PERF-01: spawn DataLoader 能启动 worker 并生成正确数据"""
    loader = create_latent_dataloader(
        params, batch_size=4, num_workers=2,
        checkpoint_path="weights/checkpoint.pth",
    )
    data_iter = iter(loader)
    prior_mu, post_mu = next(data_iter)
    assert prior_mu.shape == (4, 512)
    assert post_mu.shape == (4, 512)
    assert not torch.all(prior_mu == 0)
    assert not torch.all(post_mu == 0)
    # 清理 worker
    del data_iter, loader
```

**3. 新增吞吐量对比测试：**

```python
@pytest.mark.slow
def test_throughput_comparison(params):
    """PERF-03: num_workers=2 吞吐量 >= num_workers=0"""
    import time

    # 单 worker 基线
    loader0 = create_latent_dataloader(params, batch_size=4, num_workers=0)
    it0 = iter(loader0)
    t0 = time.time()
    for _ in range(5):
        next(it0)
    single_time = time.time() - t0

    # 双 worker
    loader2 = create_latent_dataloader(params, batch_size=4, num_workers=2)
    it2 = iter(loader2)
    t0 = time.time()
    for _ in range(5):
        next(it2)
    multi_time = time.time() - t0

    # spawn worker 有初始化开销，不要求严格更快，只要求能正常工作
    # 清理
    del it0, loader0, it2, loader2
    assert True  # 只要没报错就算通过
```

**4. 修改 dataset fixture：**
因为 __init__ 不再加载模型，将 `scope="module"` 的 dataset fixture 改为通过 loader 触发初始化：

```python
@pytest.fixture(scope="module")
def dataset(params):
    loader = create_latent_dataloader(params, batch_size=4, num_workers=0, checkpoint_path="weights/checkpoint.pth")
    data_iter = iter(loader)
    _ = next(data_iter)  # 触发 _lazy_init
    # 返回 loader 和 data_iter 供后续测试使用
    return loader, data_iter
```

然后调整使用 dataset fixture 的测试来解构 `(loader, data_iter)`。

实际上更简单的做法是：直接用 loader 取数据，不依赖 dataset fixture 的内部状态。重构所有测试为直接创建 loader 取数据：
  </action>
  <verify>
    <automated>cd /home/xyh/Symbolic_Regression/E2E/ICLR26-GenSR && python3 -m pytest tests/test_latent_dataset.py -v -x 2>&1 | head -60</automated>
  </verify>
  <done>
    - train_fm.py 的 DataLoader 创建传入 num_workers=2
    - train_fm.py 训练循环中移除 prior_mu.to(device) 和 post_mu.to(device)
    - tests/test_latent_dataset.py 所有现有测试适配 yield batch 行为并通过
    - 新增 test_spawn_dataloader 测试 spawn 多 worker 模式
    - 新增 test_throughput_comparison 测试多 worker 吞吐量
    - pytest tests/test_latent_dataset.py 全部通过
  </done>
</task>

</tasks>

<verification>
1. `python3 -m pytest tests/test_latent_dataset.py -v -x` -- 所有测试通过
2. `grep -n "num_workers" dit_train/train_fm.py` -- 确认传入 num_workers=2
3. `grep -n "to(device)" dit_train/train_fm.py` -- 确认训练循环中不再有 prior_mu.to(device) / post_mu.to(device)
4. `grep -n "multiprocessing_context" dit_train/data/latent_dataset.py` -- 确认使用 spawn
5. `grep -n "_lazy_init" dit_train/data/latent_dataset.py` -- 确认延迟初始化方法存在
6. `grep -n "batch_size=None" dit_train/data/latent_dataset.py` -- 确认跳过 collate
</verification>

<success_criteria>
- LatentPairDataset 使用 spawn 模式多进程并行生成数据
- Dataset yield 完整 batch (B, 512)，DataLoader batch_size=None 跳过 collate
- 训练循环移除 .to(device) 调用
- 所有现有测试通过 + 新增 spawn 测试通过
- grep 验证所有关键模式存在
</success_criteria>

<output>
After completion, create `.planning/phases/09-latentpairdataset/09-01-SUMMARY.md`
</output>
